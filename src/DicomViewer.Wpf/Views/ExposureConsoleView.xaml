<UserControl x:Class="DicomViewer.Wpf.Views.ExposureConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:DicomViewer.Application.Models;assembly=DicomViewer.Application"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#11161C">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EAF1F5" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Background" Value="#243341" />
            <Setter Property="Foreground" Value="#F5FAFD" />
            <Setter Property="BorderBrush" Value="#334758" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="#F5FAFD" />
            <Setter Property="Background" Value="#10171E" />
            <Setter Property="BorderBrush" Value="#334758" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="MinWidth" Value="120" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#EAF1F5" />
            <Setter Property="Margin" Value="0,4,12,0" />
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.ColumnSpan="3" Padding="20" Background="#16202A" CornerRadius="16">
                <DockPanel>
                    <StackPanel>
                        <TextBlock Text="曝光控制台" FontSize="24" FontWeight="Bold" />
                        <TextBlock Text="工作列表、联锁检查、模拟曝光、DICOM 生成已从主窗口拆分为独立页面。" Foreground="#9FB0BC" Margin="0,8,0,0" />
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Top">
                        <Button Content="加载工作列表" Command="{Binding LoadWorklistCommand}" />
                        <Button Content="联锁检查" Command="{Binding RunInterlockCheckCommand}" />
                        <Button Content="执行曝光" Command="{Binding ExecuteExposureCommand}" />
                        <Button Content="回看结果" Command="{Binding ReviewExposureCommand}" />
                        <Button Content="验证连接" Command="{Binding VerifyPacsConnectionCommand}" />
                        <Button Content="发送 PACS" Command="{Binding SendToPacsCommand}" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <StackPanel Grid.Row="1" Grid.Column="0">
                <Border Margin="0,16,0,0" Padding="16" Background="#16202A" CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="控制台状态" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding ConsoleStatusText}" Margin="0,12,0,8" FontSize="16" />
                        <TextBlock Text="当前患者" Foreground="#9FB0BC" />
                        <TextBlock Text="{Binding ConsolePatientText}" Margin="0,4,0,8" TextWrapping="Wrap" />
                        <TextBlock Text="当前检查" Foreground="#9FB0BC" />
                        <TextBlock Text="{Binding ConsoleOrderText}" Margin="0,4,0,8" TextWrapping="Wrap" />
                        <TextBlock Text="设备状态 / 流程状态" Foreground="#9FB0BC" />
                        <TextBlock Text="{Binding DeviceWorkflowText}" Margin="0,4,0,0" FontSize="16" />
                    </StackPanel>
                </Border>

                <Border Margin="0,16,0,0" Padding="16" Background="#16202A" CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="工作列表" FontSize="18" FontWeight="SemiBold" />
                        <ListBox ItemsSource="{Binding WorklistItems}"
                                 SelectedItem="{Binding SelectedWorklistItem}"
                                 Background="#10171E"
                                 BorderThickness="0"
                                 Foreground="#EAF1F5"
                                 MinHeight="260"
                                 Margin="0,12,0,0">
                            <ListBox.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:WorklistItem}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding PatientName}" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding ProcedureDescription}" Foreground="#C4D2DB" Margin="0,2,0,0" />
                                        <TextBlock Foreground="#9FB0BC" Margin="0,2,0,0">
                                            <Run Text="{Binding BodyPart}" />
                                            <Run Text=" / " />
                                            <Run Text="{Binding Projection}" />
                                            <Run Text=" / " />
                                            <Run Text="{Binding Status}" />
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Row="1" Grid.Column="2">
                <Border Margin="0,16,0,0" Padding="16" Background="#16202A" CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="PACS 配置" FontSize="18" FontWeight="SemiBold" />
                        <UniformGrid Columns="2" Margin="0,12,0,8">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="Calling AE" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding CallingAeTitleText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Called AE" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding CalledAeTitleText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                        </UniformGrid>
                        <UniformGrid Columns="2" Margin="0,0,0,8">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="Host" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding PacsHostText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Port" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding PacsPortText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                        </UniformGrid>
                        <TextBlock Text="输出目录" Foreground="#9FB0BC" />
                        <TextBox Text="{Binding OutputDirectoryText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                        <Button Content="应用 PACS 配置" Command="{Binding ApplyPacsConfigurationCommand}" Margin="0,12,0,0" />
                    </StackPanel>
                </Border>

                <Border Margin="0,16,0,0" Padding="16" Background="#16202A" CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="曝光参数" FontSize="18" FontWeight="SemiBold" />
                        <UniformGrid Columns="2" Margin="0,12,0,8">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="kV" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding KilovoltagePeakText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="mA" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding TubeCurrentMilliampereText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                        </UniformGrid>
                        <UniformGrid Columns="2" Margin="0,0,0,8">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="ms" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding ExposureTimeMillisecondsText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="mAs" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MilliampereSecondsText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                        </UniformGrid>
                        <UniformGrid Columns="2" Margin="0,0,0,8">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="SID(mm)" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding SourceToImageDistanceText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Projection" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding ProjectionText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                        </UniformGrid>
                        <TextBlock Text="BodyPart" Foreground="#9FB0BC" />
                        <TextBox Text="{Binding BodyPartText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                        <CheckBox Content="AEC 开启" IsChecked="{Binding IsAutomaticExposureControlEnabled}" Margin="0,10,0,0" />
                        <TextBlock Text="参数范围" FontSize="16" FontWeight="SemiBold" Margin="0,16,0,0" />
                        <UniformGrid Columns="2" Margin="0,12,0,8">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="kV 最小值" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MinKilovoltagePeakText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="kV 最大值" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MaxKilovoltagePeakText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                        </UniformGrid>
                        <UniformGrid Columns="2" Margin="0,0,0,8">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="mA 最小值" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MinTubeCurrentMilliampereText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="mA 最大值" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MaxTubeCurrentMilliampereText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                        </UniformGrid>
                        <UniformGrid Columns="2" Margin="0,0,0,8">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="ms 最小值" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MinExposureTimeMillisecondsText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="ms 最大值" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MaxExposureTimeMillisecondsText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                        </UniformGrid>
                        <UniformGrid Columns="2" Margin="0,0,0,8">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="mAs 最小值" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MinMilliampereSecondsText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="mAs 最大值" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MaxMilliampereSecondsText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                        </UniformGrid>
                        <UniformGrid Columns="2" Margin="0,0,0,8">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="SID 最小值(mm)" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MinSourceToImageDistanceText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="SID 最大值(mm)" Foreground="#9FB0BC" />
                                <TextBox Text="{Binding MaxSourceToImageDistanceText, UpdateSourceTrigger=PropertyChanged}" MinWidth="0" />
                            </StackPanel>
                        </UniformGrid>
                        <Button Content="应用参数和范围" Command="{Binding ApplyExposureParametersCommand}" Margin="0,12,0,0" />
                    </StackPanel>
                </Border>

                <Border Margin="0,16,0,0" Padding="16" Background="#16202A" CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="联锁状态" FontSize="18" FontWeight="SemiBold" />
                        <WrapPanel Margin="0,12,0,0">
                            <CheckBox Content="探测器已连接" IsChecked="{Binding DetectorConnected}" />
                            <CheckBox Content="球管已预热" IsChecked="{Binding TubeWarmedUp}" />
                            <CheckBox Content="防护门已关闭" IsChecked="{Binding DoorClosed}" />
                            <CheckBox Content="PACS 可用" IsChecked="{Binding PacsAvailable}" />
                        </WrapPanel>
                        <ItemsControl ItemsSource="{Binding InterlockMessages}" Margin="0,8,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Foreground="#F6C177" TextWrapping="Wrap" Margin="0,0,0,4" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="联锁通过，可执行曝光。" Foreground="#7FD38C" Margin="0,6,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasInterlockFailures}" Value="False">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <Border Margin="0,16,0,0" Padding="16" Background="#16202A" CornerRadius="12">
                    <StackPanel>
                        <TextBlock Text="模拟结果与审计" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="结果预览" Foreground="#9FB0BC" Margin="0,12,0,0" />
                        <TextBlock Text="{Binding LastExposureSummaryText}" TextWrapping="Wrap" Margin="0,4,0,10" />
                        <TextBlock Text="输出路径" Foreground="#9FB0BC" />
                        <TextBlock Text="{Binding LastArtifactPathText}" TextWrapping="Wrap" Margin="0,4,0,10" Foreground="#C4D2DB" />
                        <TextBlock Text="控制台说明" Foreground="#9FB0BC" />
                        <TextBlock Text="{Binding ConsoleNotesText}" TextWrapping="Wrap" Margin="0,4,0,10" Foreground="#C4D2DB" />
                        <TextBlock Text="PACS 发送状态" Foreground="#9FB0BC" />
                        <TextBlock Text="{Binding PacsStatusText}" Margin="0,4,0,4" />
                        <TextBlock Text="{Binding PacsDetailsText}" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="#C4D2DB" />
                        <TextBlock Text="审计日志" Foreground="#9FB0BC" />
                        <ListBox ItemsSource="{Binding AuditEntries}"
                                 Background="#10171E"
                                 BorderThickness="0"
                                 Foreground="#EAF1F5"
                                 MinHeight="120"
                                 MaxHeight="220"
                                 Margin="0,8,0,0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>